import Layout from '@theme/Layout';
import React from 'react';

const PhotoArchivePage = (props) => {
  return (
    <Layout title="Photos" description="Reunion Home Base">
      <main>
        <section>Photo Archive Page</section>
      </main>
    </Layout>
  );
};

export default PhotoArchivePage;
